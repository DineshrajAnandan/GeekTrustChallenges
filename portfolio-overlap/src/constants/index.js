const COMMANDS = require('./commands');
const MESSAGES = require('./messages');
const PATH = require('./path');
const COMMON = require('./common');

module.exports = { COMMANDS, MESSAGES, PATH, COMMON };
